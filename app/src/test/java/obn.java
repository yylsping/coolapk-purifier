import android.view.View;

import androidx.recyclerview.widget.RecyclerView;

/** Near-miss parent: binder is concrete, not abstract. */
public abstract class obn extends RecyclerView.ViewHolder {
    protected obn(View itemView) {
        super(itemView);
    }

    public void ވ(Object value) {
    }
}
