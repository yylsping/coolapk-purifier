import android.view.View;

import androidx.recyclerview.widget.RecyclerView;

/** 16.6.2 shape: Lob; — abstract reply-sponsor holder parent of qx4. */
public abstract class ob extends RecyclerView.ViewHolder {
    protected ob(View itemView) {
        super(itemView);
    }

    public abstract void ވ(Object value);
}
